# Claude Vision (primary OCR engine). Without this the server automatically
# falls back to local Tesseract OCR — the system still works, just less accurately.
ANTHROPIC_API_KEY=

PORT=8000
HTTPS_PORT=8443
